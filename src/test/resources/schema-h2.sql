CREATE TABLE IF NOT EXISTS when_rule_holidays(
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255),
    country VARCHAR(255),
    date DATE,
    is_off_day TINYINT DEFAULT 0
);
